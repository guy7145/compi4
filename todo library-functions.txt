DONE: 	(still needs to be tested)
	boolean?
	integer?
	char?
	null?
	vector?
	pair?
	procedure?
	string?
	symbol?
	rational?
	
	cons
	car
	cdr
	set-car!
	set-cdr!

	string-length
	vector-length
	
	vector-set!
	string-set!

	string-ref	
	vector-ref	
	
	char->integer
	integer->char
	zero?

	not
	eq?

	< (variadic) 
	= (variadic)
	> (variadic)
	+ (variadic)
	/ (variadic)
	* (variadic)
	- (variadic)

	denominator
	numerator
	remainder

	string->symbol
	symbol->string

	number?
	map
	list (variadic)

TODO:	scheme-	append (variadic)

	cisc- 	apply (change to tail call)

make-string
make-vector
vector
